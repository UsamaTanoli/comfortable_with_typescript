const favTransportation: string[] = ["Lx600", "Kawasaki Ninja", "tesla model x"]

for(let i = 0; i < favTransportation.length; i++){
    let massage: string = `I would like to own a: ${favTransportation[i]}`
    console.log(massage);
    
}